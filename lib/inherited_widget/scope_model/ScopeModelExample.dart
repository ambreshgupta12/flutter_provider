import 'package:flutter/material.dart';

class ScopeModelExample extends StatefulWidget {
  @override
  _ScopeModelExampleState createState() => _ScopeModelExampleState();
}

class _ScopeModelExampleState extends State<ScopeModelExample> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:new Text('Scope Model')),
      body: Column(
        children: <Widget>[

        ],
      ),
    );
  }
}
